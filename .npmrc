# Flat node_modules instead of symlinks-to-global-store. Required for
# Turbopack's strict project-root enforcement and for the Solana wallet
# adapter family (peer-dep resolution across nested deps). Trades disk
# space for compatibility — fine for this project's footprint.
node-linker=hoisted
public-hoist-pattern[]=*
